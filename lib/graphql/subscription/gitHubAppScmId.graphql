subscription OnGitHubAppScmId {
  SCMId {
    id
    login @required
    credential {
      secret @required
    }
    provider {
        apiUrl
        providerType @matches(re: "github_com")
        id
        name
    }
  }
}
