mutation SetScmProviderConfiguration($id: ID!, $name: String!, $value: String!, $description: String!) {
    setSCMProviderConfiguration(id: $id,
        item: {
            name: $name,
            description: $description,
            value: $value,
        }) {
        id
    }
}